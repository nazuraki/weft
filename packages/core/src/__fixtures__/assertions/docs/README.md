# Integration Handbook

Implement the [wire protocol](spec.md) before anything else, and return codes
from the [error registry](registry.md).

The claims this document makes about both live in its sidecar, where they can be
checked, rather than only in the prose above.

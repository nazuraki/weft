# Architecture

## Overview

The system follows a three-tier architecture.

## Data Flow

Data flows from the API layer through the service layer to the database.

See the [User listing endpoint](api.yaml) for an example.

## Database Schema

Tables are defined in the migrations directory.
